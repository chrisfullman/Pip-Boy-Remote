# Build artifacts
backend/build/
backend/build-test/
backend/build-test2/
backend/build-windows/
build/

# XMake local config and cache
.xmake/
backend/.xmake/

# XMake-generated VS project files
backend/vsxmake*/

# OS/editor files
.DS_Store

# Frontend dependencies / cache
frontend/node_modules/
frontend/.vite/
scripts/node_modules/

# Replay, development logs, and saved session gameplay WebSocket captures are ignored.
# The sample fixture is tracked, though.
logs/*.csv
logs/*.db
logs/*.jsonl
logs/*.log
logs/*.txt
!logs/sample.jsonl

# Third-party map assets (downloaded by scripts/download-assets.sh)
# These are GPL-3.0 assets from Mappalachia/Commonwealth_Cartography —
# If needed, run `download-assets.sh` to fetch them before starting the dev server.
frontend/public/maps/
